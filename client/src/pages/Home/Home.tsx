import styles from './Home.module.scss';

const Home = () => {
  return (
    <main className={styles.home}>
      <div className={styles.home__container}>Home</div>
    </main>
  );
};

export default Home;
