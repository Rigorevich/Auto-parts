import styles from './Autopart.module.scss';

const Autopart = () => {
  return (
    <main className={styles.autopart}>
      <div className={styles.autopart__container}>Autopart</div>
    </main>
  );
};

export default Autopart;
