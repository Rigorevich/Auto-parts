import styles from './Feedback.module.scss';

const Feedback = () => {
  return (
    <main className={styles.feedback}>
      <div className={styles.feedback__container}>Feedback</div>
    </main>
  );
};

export default Feedback;
